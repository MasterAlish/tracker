<div class="block">
  <div class="tasks_content">
    <% @ticket.tasks.each do |task| %>
        <%= render 'tasks/new', task: task %>
    <% end %>
  </div>
  <div class="clr"></div>
  <%= link_to 'New Task', new_task_path, remote: true, class: 'btn btn-primary btn-small' %>
</div>